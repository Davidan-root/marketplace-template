# Ejemplo de variables de entorno para la app backend
# Copia este fichero a `.env` y reemplaza los valores antes de usar
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres
# Añade otras variables necesarias: JWT_SECRET, STRIPE_KEY, etc.
JWT_SECRET=replace_me
STRIPE_SECRET_KEY=replace_me
